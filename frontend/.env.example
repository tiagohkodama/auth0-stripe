# Auth0 Configuration
# Get these from your Auth0 Dashboard: https://manage.auth0.com/
# 1. Create a Single Page Application in Auth0 Dashboard
# 2. Copy the "Domain" and "Client ID" from the application settings
# 3. Add http://localhost:3000 to "Allowed Callback URLs", "Allowed Logout URLs", and "Allowed Web Origins"
REACT_APP_AUTH0_DOMAIN=your-tenant.us.auth0.com
REACT_APP_AUTH0_CLIENT_ID=your_auth0_client_id

# Auth0 API Audience
# This should match the "Identifier" of the API you created in Auth0 Dashboard
REACT_APP_AUTH0_AUDIENCE=https://your-api-identifier

# Stripe Configuration
# Get this from your Stripe Dashboard: https://dashboard.stripe.com/
# Use your publishable key (starts with pk_test_ for test mode)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Backend API URL
# The URL where your Node.js backend is running
REACT_APP_API_URL=http://localhost:5000
